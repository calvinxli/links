"use client";
import React from "react";
import {
  Image,
  Typography,
  ConfigProvider,
  theme,
  Row,
  Tooltip,
  Button,
} from "antd";
import { PageLayout } from "@/components/PageLayout";

const { Text, Title } = Typography;

const ProductsPage = () => <PageLayout content={<Button>Testing</Button>} />;

export default ProductsPage;
